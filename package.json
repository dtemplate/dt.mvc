{
  "name": "dt.mvc",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^2.0.15"
  },
  "scripts": {
    "dev": "nodemon --watch src --exec dt --run ."
  },
  "dependencies": {
    "npm-add-script": "^1.1.0"
  }
}
